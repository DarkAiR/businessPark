{% block importBlock %}
    {{ import('css','menu.css') }}
    {{ import('css','pageMapMenu.css','ext.widgets.css') }}
{% endblock %}

<div class='page-map-menu-widget  hmenu'>
    <ul>
        {% for item in items %}
            {% set active = item.select %}
            {% set posClass = (loop.first ? 'first' : '') ~ (loop.last ? ' last' : '') %}
            {% set blank = item.blank ? 'target="_blank"' : '' %}
            <li class='{{posClass}}'>

                <table><tr>
                    <td class='image'>
                        {% set w = const('MenuItem', 'IMAGE_W') %}
                        {% set h = const('MenuItem', 'IMAGE_H') %}

                        {% if item.iconUrl %}
                            <img src='{{ item.iconUrl }}' width="{{w}}px" height="{{h}}px">
                        {% else %}
                            <div style='width:{{w}}px; height:{{h}}px'></div>
                        {% endif %}
                    </td>
                    <td class='text'>
                        <a class='{% if active %}active{% endif %}' href='{{ item.link }}' {{blank}}>{{ item.name|raw }}</a>
                    </td>
                </tr></table>
            </li>
        {% endfor %}
    </ul>
</div>



