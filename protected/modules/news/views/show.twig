{% extends 'modules/news/views/base.twig' %}

{% block js %}
    {{ parent() }}
    <script type="text/javascript">
    function backAway()
    {
        // if it was the first page
        if (document.referrer == '')
            window.location = "{{backUrl}}";
        else
            window.location = document.referrer;
    }
    </script>
{% endblock %}

{% block css %}
    {{ parent() }}
    {{ import('css','show.css', 'application.modules.news.css') }}
{% endblock %}

{% block newsBack %}
    <div class='backBtnCont'>
        <a class='backBtn' href="#" onclick="backAway(); return false;">&larr; Вернуться</a>
    </div>
{% endblock %}

{% block newsLeftCol %}
{% endblock %}

{% block newsContent %}
    <div class='content'>
        <div class='date'>{{ static('DateHelper', 'formatNewsDate', [news.createTime]) }}</div>
        <div class='title'>{{ news.title|raw }}</div>
        <div class='text'>{{ news.desc|raw }}</div>
    </div>
    <div class='navigation'>
        <div class='prev'>
            {% if prevLink %}
                <a href='{{ prevLink }}'>&larr; Предыдущая новость</a>
            {% else %}
                <div class='inactive'>&larr; Предыдущая новость</div>
            {% endif %}
        </div>
        <div class='next'>
            {% if nextLink %}
                <a href='{{ nextLink }}'>Следующая новость &rarr;</a>
            {% else %}
                <div class='inactive'>Следующая новость &rarr;</div>
            {% endif %}
        </div>
    </div>
{% endblock %}